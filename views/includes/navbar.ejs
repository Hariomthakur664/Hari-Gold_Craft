<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top" style="min-height: 70px;">
    <div class="container-fluid px-1">
        <!-- Brand and logo -->
        <a class="navbar-brand d-flex align-items-center ms-0" href="/hr" style="font-size: 1rem; margin-right: 1px;" >
            <img src="/images/logo.jpg" alt="Logo" class="img-fluid" style="width: 40px; height: 40px; border-radius: 50%; margin-right: 8px;">
        </a>

         <!-- Search Form -->
         <form class="d-flex ms-auto me-auto ms-0" style="max-width: 220px; width: 100%;">
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="search" style="min-width: 120px; font-size: 0.85rem;">
            <button class="btn btn-outline-success btn-sm" type="submit">Search</button>
        </form>

        <!-- General Navigation Toggler -->
        <button class="navbar-toggler ms-0" type="button" data-bs-toggle="collapse" data-bs-target="#generalNav" aria-controls="generalNav" aria-expanded="false" aria-label="Toggle navigation" >
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- User Toggler -->
        <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#userNav" aria-controls="userNav" aria-expanded="false" aria-label="Toggle user options">
            <i class="fa-solid fa-user-plus"></i>
        </button>

        <!-- General Navbar Links -->
        <div class="collapse navbar-collapse" id="generalNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <% if(currUser && currAdmin) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/hr/listing/new">Add Jewelry Shop</a>
                </li>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link" href="/hr/listing">Jewelry Shops</a>
                </li>
            </ul>
        </div>

        <!-- User Menu Dropdown -->
        <div class="collapse navbar-collapse" id="userNav">
            
                    <ul class="nav-item dropdown dropdown-menu dropdown-menu-end navbar-nav ms-1 " style="width: 0.85rem;">
                        <% if(!currUser) { %>
                        <li>
                            <a class="dropdown-item" href="/login">
                                <i class="fa-solid fa-user me-2"></i>User Login
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="/adminLogin">
                                <i class="fa-solid fa-user-tie me-2"></i>Admin Login
                            </a>
                        </li>
                        <% } %>

                        <% if(currUser && !currAdmin) { %>
                        <li>
                            <a class="dropdown-item" href="/logout">
                                <i class="fa-solid fa-user me-2"></i>Log-Out
                            </a>
                        </li>
                        <% } %>

                        <% if(currUser && currAdmin) { %>
                        <li>
                            <a class="dropdown-item" href="/a-logout">
                                <i class="fa-solid fa-user-tie me-2"></i>Log-Out
                            </a>
                        </li>
                        <% } %>
                    </ul>
        </div>
    </div>
</nav>
